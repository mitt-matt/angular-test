import { BoldNumberPipe } from './bold-number.pipe';

describe('BoldNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new BoldNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
